extends ../baseEmail

block content
  p Dear #{payload.sender.firstname?payload.sender.firstname:null},
  p We regret to inform you that your friendship invitation on #{payload.appInfo.name} has been declined by #{payload.receiver.firstname + ' ' + payload.receiver.lastname}. Keep exploring and connecting with others on our platform.

  p Thank you for being a part of the #{payload.appInfo.name} community. Keep connecting, and we wish you all the best in finding new friendships.

  br
  small
    b - #{payload.appInfo.email}, #{payload.appInfo.name}