extends baseEmail

block content
  p Hi #{payload.user.name},
  p Welcome to #{payload.appInfo.name}, we're glad to have you 🎉🙏
  p We're all a big family here, Your World Of #{payload.appInfo.name} Await We’re so glad you’re here! You are now part of a growing community of professionals creating, collaborating, and connecting across the globe via #{payload.appInfo.name}.
  p Whether you’ve joined to create something of your own or just to connect with other professionals, we’ve got something for you. Let’s go!
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  b
                   a(href=`${payload.url}`, target='_blank') Click to explore
  p If you need any help with registering on #{payload.appInfo.name}, please don't hesitate to contact us!
  p - #{payload.appInfo.email}, #{payload.appInfo.name}